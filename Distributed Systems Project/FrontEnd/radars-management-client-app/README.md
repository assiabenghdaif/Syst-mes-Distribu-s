# RadarsManagementClientApp
## install required 
Run `npm install`

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:5555/`. The application will automatically reload if you change any of the source files.

